export function generateApi(spec: string) {
    // parse, convert to OpenAPI3 & deref (?)
    // return a JSON object
}

export function generateApis() {
    // Get all relevant files from the directory
    // For each one, generateApi
    // Output into the apis dir
}